import { Resource } from "i18next";

import { enUS } from "@app/translations/languages/enUS";
import { ptBR } from "@app/translations/languages/ptBR";

const translations: Resource = {
    ptBR,
    enUS,
};

export { translations };
